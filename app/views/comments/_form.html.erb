<%= form_for [commentable, Comment.new] do |f| %>
  <h4 class="f7 fw6 ttu black pa0">Leave a comment</h4>
  <%= f.text_area :body, class: "pa2 input-reset ba b--light-gray bg-white h4 fw4 w-100", placeholder: "Want to leave a comment?" %>
  <div class="tr">
  	<%= f.submit "Submit", class: "link pointer white bg-orange pa2 b--none br2 hover-bg-light-red" %>
  </div>
<% end %>
